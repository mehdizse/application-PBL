---
title: "Écrire et Créer - Blog Éducatif"
---

<header class="sticky top-0 z-50 bg-gradient-to-r from-blue-600 via-purple-500 to-pink-500 shadow-lg">
  <nav class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
    <!-- Logo Section -->
    <a href="/" class="flex items-center space-x-2 hover:opacity-90 transition-opacity">
      <div class="w-10 h-10 bg-white rounded-full flex items-center justify-center shadow-md">
        <span class="text-xl">✍️</span>
      </div>
      <div class="hidden sm:block">
        <h1 class="text-lg md:text-xl font-bold text-white leading-tight">Écrire & Créer</h1>
        <p class="text-blue-100 text-xs">Apprentissage par projet</p>
      </div>
    </a>

    <!-- Desktop Navigation -->
    <ul class="hidden lg:flex space-x-6 items-center text-sm">
      <li><a href="/" class="text-white hover:text-blue-200 font-semibold transition-colors">Accueil</a></li>
      <li><a href="/about" class="text-white hover:text-blue-200 font-semibold transition-colors">À propos</a></li>
      <li><a href="/resources" class="text-white hover:text-blue-200 font-semibold transition-colors">📚 Ressources</a></li>
      <li><a href="/gallery" class="text-white hover:text-blue-200 font-semibold transition-colors">🌟 Galerie</a></li>
      <li><a href="/posts/welcome-to-project-based-writing" class="text-white hover:text-blue-200 font-semibold transition-colors">Blog</a></li>
    </ul>

    <!-- Auth & Admin Buttons -->
    <div class="flex items-center space-x-3">
      <li id="auth-link" class="list-none">
        <a href="/login" class="text-white hover:text-blue-200 font-semibold text-sm transition-colors">🔐 Connexion</a>
      </li>
      <a href="/admin" class="bg-white text-blue-600 px-3 py-2 rounded-lg hover:bg-blue-50 font-semibold text-sm transition-colors">
        🎨 Admin
      </a>
    </div>

    <!-- Mobile menu button -->
    <button class="lg:hidden text-white text-2xl hover:text-blue-200 transition-colors" aria-label="Menu">
      ☰
    </button>
  </nav>
</header>

<script>
  import { auth } from '../lib/firebase.js';
  import { onAuthStateChanged, signOut } from 'firebase/auth';

  onAuthStateChanged(auth, (user) => {
    const authLink = document.getElementById('auth-link');
    if (authLink) {
      if (user) {
        // User is logged in - show Logout button
        authLink.innerHTML = `<button id="logout-header-btn" class="text-white hover:text-blue-200 font-semibold transition-colors duration-200">🚪 Déconnexion (${user.displayName || user.email})</button>`;
        
        const logoutBtn = document.getElementById('logout-header-btn');
        logoutBtn.addEventListener('click', async () => {
          try {
            await signOut(auth);
            window.location.href = '/';
          } catch (error) {
            console.error('Erreur de déconnexion:', error);
          }
        });
      } else {
        // User is logged out - show Login link
        authLink.innerHTML = '<a href="/login" class="text-white hover:text-blue-200 font-semibold transition-colors duration-200">🔐 Connexion</a>';
      }
    }
  });
</script>