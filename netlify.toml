[build]
  command = "npm run build"
  publish = "dist"

# Ignore secrets scan - Firebase API keys are public and safe
[build.environment]
  SECRETS_SCAN_ENABLED = "false"

[dev]
  command = "npm run dev"
  port = 3000

[functions]
  directory = "netlify/functions"